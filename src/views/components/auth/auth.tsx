import React from 'react'

const Auth = () => {
    return (
        <div style={{
            background: "transparent",
            width: "100%",
            height: "100%"
        }}>
            Auth
        </div>
    );
}

export default Auth;